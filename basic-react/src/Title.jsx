function Title({title}){
    return (
        <>
        <h2>{title}</h2>
        </>
    )
}
//i know here is small content so there is no need to write indivisual but it is best practice to structure componets like this

export default Title;